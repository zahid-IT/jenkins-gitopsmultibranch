apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/zahid-IT/jenkins-gitopsmultibranch.git'
    targetRevision: dev
    path: helm-chart/nginx-app
    helm:
      valueFiles:
        - values/dev-values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
